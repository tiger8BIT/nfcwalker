# JWT
JWT_SECRET=changeThisInProduction

# Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email settings
MAIL_FROM=noreply@nfcwalker.com
APP_NAME=NFC Walker

# SMTP settings (configure your real SMTP server)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_AUTH=true
SMTP_STARTTLS=true
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Database (postgres in docker-compose)
JDBC_URL=jdbc:postgresql://postgres:5432/nfcwalker
JDBC_USER=postgres
JDBC_PASSWORD=postgres

# Application
FRONTEND_URL=http://localhost:3000
APP_CHALLENGE_SECRET=changeThisInProduction

