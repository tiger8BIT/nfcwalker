# ==============================================================================
# LOCAL DEVELOPMENT SETTINGS (Ready to use)
# ==============================================================================

# Security: Default dev secrets (auto-generated)
# NOTE: For production, generate new secrets using: openssl rand -base64 32
JWT_SECRET=ttOIEgp3W4U8/NP+diGxKzgtZr5saqliJmBLN4xI9GA=
APP_CHALLENGE_SECRET=Ro0LsbcINMKmaLDNBrfeEQdGFhCuuBubmx5DenCe6lA=

# Google OAuth2: Get from Google Cloud Console (Required for actual login)
GOOGLE_CLIENT_ID=<REPLACE_WITH_GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<REPLACE_WITH_GOOGLE_CLIENT_SECRET>

# SMTP: Credentials for sending emails (Required for invitations)
SMTP_USERNAME=<REPLACE_WITH_SMTP_USERNAME>
SMTP_PASSWORD=<REPLACE_WITH_SMTP_PASSWORD>

# ==============================================================================
# INFRASTRUCTURE DEFAULTS (Pre-configured for Docker Compose)
# ==============================================================================

APP_NAME=NFCWalker
FRONTEND_URL=http://localhost:3000

# Email Settings
MAIL_FROM=no-reply@nfcwalker.com
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_AUTH=false
SMTP_STARTTLS=false

# Database Settings (Matches docker-compose.yml)
JDBC_URL=jdbc:postgresql://postgres:5432/nfcwalker
JDBC_USER=postgres
JDBC_PASSWORD=postgres
