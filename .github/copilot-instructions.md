to run any gradlew or java related command use
patter [cd /Users/vyacheslavkolodynskiy/IdeaProjects/nfcwalker && export JAVA_HOME=$(/usr/libexec/java_home -v 21) && ./gradlew <command>]
if you run tests then prefer read results in generated html report, especially instead of re-running tests
