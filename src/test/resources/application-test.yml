micronaut:
  application:
    name: nfcwalker
  test-resources:
    containers:
      postgres:
        image-name: postgres:15

datasources:
  default:
    db-type: postgres

app:
  challenge:
    secret: test-secret-for-challenges
    ttl-seconds: 60

